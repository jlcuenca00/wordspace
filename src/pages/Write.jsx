import{useEffect,useState}from'react';export default function Write(){const[title,setTitle]=useState(()=>localStorage.wsTitle||''),[text,setText]=useState(()=>localStorage.wsDraft||'');useEffect(()=>{localStorage.wsTitle=title;localStorage.wsDraft=text},[title,text]);let words=text.trim()?text.trim().split(/\s+/).length:0;return <main className="write"><div className="writeMeta"><span>WRITE / DRAFT</span><span>{words.toLocaleString()} WORDS</span></div><article><input className="title" placeholder="UNTITLED" value={title} onChange={e=>setTitle(e.target.value)}/><textarea autoFocus placeholder="Begin." value={text} onChange={e=>setText(e.target.value)}/></article><div className="save">SAVED LOCALLY · {text.length} CHARACTERS</div></main>}
